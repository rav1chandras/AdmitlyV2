# ── Dependencies ──
node_modules/

# ── Next.js build ──
.next/
out/

# ── Environment & secrets ──
.env
.env.local
.env.production
.env.staging
*.pem

# ── OS files ──
.DS_Store
Thumbs.db
__MACOSX/

# ── IDE ──
.idea/
.vscode/
*.swp
*.swo

# ── TypeScript build info ──
tsconfig.tsbuildinfo

# ── Docker data (never commit volumes) ──
postgres_data/

# ── Debug / logs ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.bak

# ── Local snapshots (don't commit zipped backups) ──
snapshots/
*.zip
